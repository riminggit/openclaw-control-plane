# OpenClaw Control Plane 全功能测试用例

> 版本: v0.1.0-post-bugfix  
> 生成时间: 2026-04-01  
> 后端地址: http://localhost:8000  
> 标注「已验证」的用例表示后端 API 已通过 curl 确认可用

---

## 1. 全局/基础

### TC-001: 所有页面路由正常访问
- **模块**: 全局/路由
- **优先级**: P0
- **前置条件**: 前端已启动，后端运行在 localhost:8000
- **测试步骤**:
  1. 在浏览器中依次访问以下路由: `/`, `/sessions`, `/sessions/xxx`, `/cron`, `/chat`, `/settings`, `/kanban`, `/tasks`, `/tasks/xxx`, `/projects`, `/projects/xxx`, `/analytics/cost`, `/agents/lifecycle`, `/agents-mgmt`, `/channels`, `/logs`, `/services`, `/skills`, `/memory`, `/usage`, `/security`, `/extensions`, `/communication`
  2. 观察每个页面是否正常渲染（无白屏、无崩溃、无404）
- **预期结果**: 所有22个路由页面正常渲染，侧边栏导航正确高亮当前页面，控制台无致命错误

### TC-002: 主题切换功能
- **模块**: 全局/主题
- **优先级**: P1
- **前置条件**: 前端已启动
- **测试步骤**:
  1. 找到主题切换入口（通常在顶栏或设置面板）
  2. 依次切换到 dark / light / cyberpunk / forest / ocean 5个主题
  3. 每次切换后检查页面配色、背景色、组件样式是否正确变化
  4. 刷新页面，验证主题设置是否持久化（localStorage）
- **预期结果**: 5个主题均能正确切换，配色方案各不相同，刷新后保持用户选择的主题

### TC-003: 中英文语言切换
- **模块**: 全局/i18n
- **优先级**: P1
- **前置条件**: 前端已启动
- **测试步骤**:
  1. 找到语言切换入口
  2. 切换到英文，检查页面文字是否全部变为英文
  3. 切换回中文，检查页面文字是否恢复中文
  4. 检查侧边栏、按钮、表格表头、空状态提示、错误消息等是否均已翻译
- **预期结果**: 中英文切换正常，所有UI文案随语言正确切换，无遗漏未翻译的文字

### TC-004: 侧边栏折叠/展开
- **模块**: 全局/布局
- **优先级**: P1
- **前置条件**: 前端已启动
- **测试步骤**:
  1. 点击侧边栏折叠按钮
  2. 确认侧边栏收起，仅显示图标
  3. 鼠标悬停在图标上，确认弹出 tooltip 显示菜单名称
  4. 再次点击展开按钮，确认侧边栏恢复完整显示
- **预期结果**: 折叠/展开功能正常，过渡动画流畅，折叠状态下图标可点击跳转

### TC-005: 响应式布局
- **模块**: 全局/响应式
- **优先级**: P1
- **前置条件**: 前端已启动
- **测试步骤**:
  1. 在桌面端（1920px+）查看布局是否正常
  2. 缩小浏览器窗口至 768px（平板），检查侧边栏是否自动折叠
  3. 缩小至 375px（手机），检查布局是否仍可正常使用
  4. 检查表格、卡片、模态框在不同宽度下是否出现溢出或遮挡
- **预期结果**: 在桌面端/平板/手机三种宽度下布局正常，无溢出、无遮挡、操作可用

---

## 2. Dashboard (/)

### TC-006: Dashboard 页面加载与统计卡片展示
- **模块**: Dashboard
- **优先级**: P0
- **前置条件**: 前端已启动，Gateway 已连接
- **测试步骤**:
  1. 访问 `/`
  2. 检查 Gateway 连接状态是否显示为"已连接"
  3. 检查是否有统计卡片展示（会话数、任务数等）
  4. 检查最近会话列表是否加载
  5. 检查 Cron 任务数量是否显示
- **预期结果**: 页面正常加载，Gateway 连接成功后显示统计数据卡片和最近会话列表，无空白或加载异常

### TC-007: Dashboard 数据自动刷新
- **模块**: Dashboard
- **优先级**: P1
- **前置条件**: Gateway 已连接，Dashboard 已加载
- **测试步骤**:
  1. 在 Dashboard 页面停留
  2. 通过其他途径（如 API 调用）创建一个新会话
  3. 等待自动刷新周期
  4. 检查 Dashboard 是否自动更新了会话数据
- **预期结果**: Dashboard 在自动刷新周期后（通常30秒）更新显示最新数据

### TC-008: Dashboard Gateway 断连处理
- **模块**: Dashboard
- **优先级**: P1
- **前置条件**: Dashboard 已加载
- **测试步骤**:
  1. 停止后端 Gateway WebSocket 代理
  2. 观察 Dashboard 连接状态变化
  3. 恢复 Gateway，观察是否自动重连
- **预期结果**: 断连时显示"断开"或"连接中"状态，数据不崩溃；恢复后自动重连并刷新数据

---

## 3. Sessions (/sessions)

### TC-009: 会话列表加载
- **模块**: Sessions
- **优先级**: P0
- **前置条件**: Gateway 已连接，存在至少1个会话
- **测试步骤**:
  1. 访问 `/sessions`
  2. 等待数据加载完成
  3. 检查会话列表是否显示，每条会话应包含 key、agent、创建时间等信息
  4. 检查分页或"加载更多"功能
- **预期结果**: 会话列表正确加载，数据格式合理，分页功能正常

### TC-010: 会话详情页
- **模块**: Sessions
- **优先级**: P0
- **前置条件**: 存在至少1个会话
- **测试步骤**:
  1. 在会话列表中点击某个会话
  2. 检查是否跳转到 `/sessions/:key`
  3. 检查会话详情页是否正确展示会话历史消息
  4. 检查返回按钮是否可用
- **预期结果**: 正确跳转到详情页，展示该会话的完整历史记录，返回按钮功能正常

### TC-011: 会话列表空状态与断连处理
- **模块**: Sessions
- **优先级**: P1
- **前置条件**: Gateway 未连接或无会话
- **测试步骤**:
  1. 在 Gateway 未连接状态下访问 `/sessions`
  2. 检查是否有合理的空状态提示或连接状态提示
  3. 恢复连接后检查列表是否自动加载
- **预期结果**: 未连接时显示连接提示，无会话时显示空状态，不会白屏或报错

---

## 4. Cron (/cron)

### TC-012: Cron 任务列表加载
- **模块**: Cron
- **优先级**: P0
- **前置条件**: Gateway 已连接
- **测试步骤**:
  1. 访问 `/cron`
  2. 等待 Gateway RPC 返回 cron 列表
  3. 检查 Cron 任务列表是否正确展示
  4. 检查是否显示任务总数
- **预期结果**: Cron 任务列表正确展示，包含 schedule、message、enabled 等字段，总数正确

### TC-013: 新建 Cron 任务
- **模块**: Cron
- **优先级**: P0
- **前置条件**: Gateway 已连接
- **测试步骤**:
  1. 点击"新建"Cron 按钮
  2. 填写 schedule（如 `*/5 * * * *`）、message 等字段
  3. 提交表单
  4. 检查列表是否新增该任务
- **预期结果**: 新任务创建成功并出现在列表中，表单验证正常（schedule 格式校验）

### TC-014: 删除 Cron 任务
- **模块**: Cron
- **优先级**: P0
- **前置条件**: 存在至少1个 Cron 任务
- **测试步骤**:
  1. 在 Cron 列表中找到要删除的任务
  2. 点击删除按钮
  3. 确认删除
  4. 检查列表是否移除了该任务
- **预期结果**: 任务删除成功，列表即时更新

### TC-015: 启用/禁用 Cron 任务
- **模块**: Cron
- **优先级**: P1
- **前置条件**: 存在至少1个 Cron 任务
- **测试步骤**:
  1. 点击任务的启用/禁用开关
  2. 检查任务状态是否切换
  3. 再次点击恢复原状态
- **预期结果**: 状态切换即时生效，UI 正确反映当前启用状态

---

## 5. Chat (/chat)

### TC-016: Chat WebSocket 连接
- **模块**: Chat
- **优先级**: P0
- **前置条件**: 前端已启动，后端运行
- **测试步骤**:
  1. 访问 `/chat`
  2. 检查 WebSocket 是否自动连接（通过 `/ws/gateway` 代理）
  3. 检查连接状态指示器是否显示"已连接"
- **预期结果**: WebSocket 自动建立连接，状态指示正确

### TC-017: 消息发送与接收
- **模块**: Chat
- **优先级**: P0
- **前置条件**: WebSocket 已连接
- **测试步骤**:
  1. 在输入框中输入消息并发送
  2. 检查消息是否出现在聊天窗口
  3. 检查是否有响应消息返回
  4. 检查消息时间戳是否正确
- **预期结果**: 消息发送成功，响应正常展示，消息流按时间顺序排列

### TC-018: 聊天历史与搜索
- **模块**: Chat
- **优先级**: P1
- **前置条件**: 存在聊天记录
- **测试步骤**:
  1. 打开 Chat 页面，检查历史消息是否自动加载
  2. 使用搜索功能搜索关键词
  3. 检查搜索结果是否正确过滤
  4. 检查书签/收藏功能
- **预期结果**: 历史消息正确加载，搜索功能正常，书签功能可用

---

## 6. Gateway Settings (/settings)

### TC-019: 配置项展示
- **模块**: Gateway Settings
- **优先级**: P1
- **前置条件**: 前端已启动
- **测试步骤**:
  1. 访问 `/settings`
  2. 检查配置项是否全部展示
  3. 检查配置项是否有合理的分组/分类
  4. 检查当前值是否正确显示
- **预期结果**: 所有 Gateway 配置项正确展示，当前值与实际配置一致

### TC-020: 配置修改
- **模块**: Gateway Settings
- **优先级**: P1
- **前置条件**: 已有可修改的配置项
- **测试步骤**:
  1. 修改某项配置值
  2. 保存配置
  3. 刷新页面，检查修改是否持久化
  4. 检查配置 Diff 功能是否正常显示变更
- **预期结果**: 配置修改成功保存，刷新后保持修改值，Diff 正确展示变更内容

### TC-021: 配置备份与恢复
- **模块**: Gateway Settings
- **优先级**: P2
- **前置条件**: 配置页面已加载
- **测试步骤**:
  1. 点击备份按钮，创建一个配置备份
  2. 修改当前配置
  3. 从备份恢复配置
  4. 检查配置是否恢复到备份时的状态
- **预期结果**: 备份创建成功，恢复操作正确还原配置

---

## 7. Kanban (/kanban)

### TC-022: 看板视图展示
- **模块**: Kanban
- **优先级**: P1
- **前置条件**: 存在任务数据，Gateway 已连接
- **测试步骤**:
  1. 访问 `/kanban`
  2. 等待数据同步完成
  3. 检查是否按状态列展示任务（如 planned / in-progress / done）
  4. 检查每个卡片是否显示任务标题、优先级等关键信息
- **预期结果**: 看板按列正确展示，每张任务卡片信息完整，自动刷新（30s 间隔）

### TC-023: 任务拖拽移动
- **模块**: Kanban
- **优先级**: P0
- **前置条件**: Kanban 已加载，存在多列任务
- **测试步骤**:
  1. 将一张任务卡片从"计划"列拖拽到"进行中"列
  2. 检查拖拽交互是否流畅
  3. 检查拖拽后任务状态是否更新
  4. 刷新页面确认状态持久化
- **预期结果**: 拖拽操作成功，任务状态更新并持久化

### TC-024: Kanban 数据同步
- **模块**: Kanban
- **优先级**: P1
- **前置条件**: Kanban 已加载
- **测试步骤**:
  1. 点击同步按钮
  2. 检查同步是否成功
  3. 检查是否正确获取 Gateway 卡片数据
- **预期结果**: 同步功能正常，同步后数据与 Gateway 一致

---

## 8. Tasks (/tasks)

### TC-025: 任务列表加载 (已验证 ✅)
- **模块**: Tasks
- **优先级**: P0
- **前置条件**: 后端运行，存在任务数据
- **测试步骤**:
  1. 访问 `/tasks`
  2. 检查任务列表是否正确加载
  3. 检查任务卡片/表格是否包含标题、状态、优先级、所属项目等字段
  4. 验证 API: `curl http://localhost:8000/api/tasks` 返回 200
- **预期结果**: 任务列表正确展示，数据与后端一致，支持筛选和分页

### TC-026: 新建任务 (已验证 ✅)
- **模块**: Tasks
- **优先级**: P0
- **前置条件**: 存在至少1个项目
- **测试步骤**:
  1. 点击"新建任务"按钮
  2. 填写必填字段（标题、所属项目）
  3. 设置可选字段（描述、优先级、状态、分类）
  4. 提交表单
  5. 验证 API: `curl -X POST http://localhost:8000/api/tasks` 返回 201
- **预期结果**: 任务创建成功，出现在列表中，表单验证正确（空标题应报错）

### TC-027: 任务编辑（状态、标题、描述）
- **模块**: Tasks
- **优先级**: P0
- **前置条件**: 存在至少1个任务
- **测试步骤**:
  1. 点击任务的编辑按钮
  2. 修改任务标题
  3. 修改任务状态（如 planned → in-progress）
  4. 修改任务描述
  5. 保存修改
  6. 验证 API: `curl -X PUT http://localhost:8000/api/tasks/{id}` 返回 200
- **预期结果**: 修改成功保存，列表和详情页即时更新

### TC-028: 任务删除 (已验证 ✅)
- **模块**: Tasks
- **优先级**: P0
- **前置条件**: 存在至少1个任务
- **测试步骤**:
  1. 点击任务的删除按钮
  2. 确认删除操作
  3. 检查列表是否移除该任务
  4. 验证 API: `curl -X DELETE http://localhost:8000/api/tasks/{id}` 返回 204
- **预期结果**: 任务删除成功，列表即时更新

### TC-029: 任务详情页 (已验证 ✅)
- **模块**: Tasks
- **优先级**: P0
- **前置条件**: 存在至少1个任务
- **测试步骤**:
  1. 在任务列表中点击某个任务
  2. 检查是否跳转到 `/tasks/:id`
  3. 检查详情页是否正确展示所有任务字段
  4. 验证 API: `curl http://localhost:8000/api/tasks/{id}` 返回 200
- **预期结果**: 详情页正确展示任务的所有字段，数据与列表一致

---

## 9. Projects (/projects)

### TC-030: 项目列表加载 (已验证 ✅)
- **模块**: Projects
- **优先级**: P0
- **前置条件**: 后端运行
- **测试步骤**:
  1. 访问 `/projects`
  2. 检查项目列表是否正确加载
  3. 检查每个项目是否显示名称、code、状态、任务数等
  4. 验证 API: `curl http://localhost:8000/api/projects` 返回 200
- **预期结果**: 项目列表正确展示，包含任务数量和阻塞任务数量统计

### TC-031: 项目详情页 (已验证 ✅)
- **模块**: Projects
- **优先级**: P0
- **前置条件**: 存在至少1个项目
- **测试步骤**:
  1. 在项目列表中点击某个项目
  2. 检查是否跳转到 `/projects/:id`
  3. 检查详情页是否展示项目信息和关联任务列表
  4. 验证 API: `curl http://localhost:8000/api/projects/{id}` 返回 200
- **预期结果**: 项目详情正确展示，关联任务列表正确加载

### TC-032: 项目新建与删除 (已验证 ✅)
- **模块**: Projects
- **优先级**: P1
- **前置条件**: 后端运行
- **测试步骤**:
  1. 新建一个项目（填写 name 和 code）
  2. 验证 API: `curl -X POST http://localhost:8000/api/projects` 返回 201
  3. 删除该项目
  4. 验证 API: `curl -X DELETE http://localhost:8000/api/projects/{id}` 返回 204
  5. 检查重复 code 是否返回 409
- **预期结果**: 新建成功，删除成功，重复 code 被拒绝

---

## 10. Agents Management (/agents-mgmt)

### TC-033: Agent 列表展示 (已验证 ✅)
- **模块**: Agents Management
- **优先级**: P0
- **前置条件**: 后端运行
- **测试步骤**:
  1. 访问 `/agents-mgmt`
  2. 等待 Agent 列表加载
  3. 检查是否展示真实 Agent 数据（名称、角色、状态等）
  4. 验证 API: `curl http://localhost:8000/api/agents-mgmt/list` 返回 200
- **预期结果**: Agent 列表正确展示，包含真实数据，无模拟数据

### TC-034: Agent 模板查看 (已验证 ✅)
- **模块**: Agents Management
- **优先级**: P1
- **前置条件**: 后端运行
- **测试步骤**:
  1. 在 Agent 管理页面查找模板功能
  2. 验证 API: `curl http://localhost:8000/api/agents-mgmt/templates` 返回 200
  3. 检查模板数据格式是否合理
- **预期结果**: Agent 模板列表正确返回，数据格式合理

### TC-035: Agent CRUD 操作
- **模块**: Agents Management
- **优先级**: P1
- **前置条件**: 后端运行
- **测试步骤**:
  1. 点击"新建 Agent"按钮，填写表单并提交
  2. 验证 API: `curl -X POST http://localhost:8000/api/agents-mgmt` 返回结果
  3. 修改某个 Agent 的信息
  4. 验证 API: `curl -X PATCH http://localhost:8000/api/agents-mgmt/{id}` 返回结果
  5. 删除某个 Agent
  6. 验证 API: `curl -X DELETE http://localhost:8000/api/agents-mgmt/{id}` 返回结果
- **预期结果**: 增删改操作均成功，列表即时更新

### TC-036: Agent 测试运行
- **模块**: Agents Management
- **优先级**: P2
- **前置条件**: 存在至少1个 Agent
- **测试步骤**:
  1. 在 Agent 列表中找到测试按钮
  2. 点击测试运行
  3. 验证 API: `curl -X POST http://localhost:8000/api/agents-mgmt/{id}/test` 返回结果
- **预期结果**: 测试请求发送成功，返回测试结果或状态

---

## 11. Agent Lifecycle (/agents/lifecycle)

### TC-037: Agent 生命周期展示 (已验证 ✅)
- **模块**: Agent Lifecycle
- **优先级**: P1
- **前置条件**: 后端运行
- **测试步骤**:
  1. 访问 `/agents/lifecycle`
  2. 检查是否展示 Agent 生命周期数据（状态、运行时间等）
  3. 检查自动刷新是否工作（30s 间隔）
  4. 验证 API: `curl http://localhost:8000/api/agents/lifecycle` 返回 200
- **预期结果**: 生命周期数据正确展示，自动刷新正常

### TC-038: Agent 清理操作
- **模块**: Agent Lifecycle
- **优先级**: P1
- **前置条件**: Agent Lifecycle 页面已加载
- **测试步骤**:
  1. 执行手动清理操作
  2. 验证 API: `curl -X POST http://localhost:8000/api/agents/lifecycle/cleanup`
  3. 配置自动清理
  4. 验证 API: `curl -X POST http://localhost:8000/api/agents/lifecycle/cleanup/auto`
- **预期结果**: 手动清理和自动清理配置均正常工作

### TC-039: Lifecycle 配置查看与修改
- **模块**: Agent Lifecycle
- **优先级**: P2
- **前置条件**: Agent Lifecycle 页面已加载
- **测试步骤**:
  1. 查看当前清理配置
  2. 修改清理配置参数
  3. 刷新页面确认持久化
- **预期结果**: 配置正确展示和持久化

---

## 12. Channels (/channels)

### TC-040: 渠道列表加载 (已验证 ✅)
- **模块**: Channels
- **优先级**: P0
- **前置条件**: 后端运行
- **测试步骤**:
  1. 访问 `/channels`
  2. 检查渠道列表是否正确展示
  3. 检查每个渠道是否显示类型和状态
  4. 验证 API: `curl http://localhost:8000/api/channels/list` 返回 200
- **预期结果**: 渠道列表正确展示，包含渠道类型和状态信息

### TC-041: 渠道状态检查 (已验证 ✅)
- **模块**: Channels
- **优先级**: P1
- **前置条件**: 后端运行
- **测试步骤**:
  1. 验证 API: `curl http://localhost:8000/api/channels/status` 返回 200
  2. 检查每个渠道的在线/离线状态
  3. 检查前端是否正确解析并展示渠道状态
- **预期结果**: 渠道状态正确展示，在线渠道显示为已连接

### TC-042: 渠道配置编辑
- **模块**: Channels
- **优先级**: P1
- **前置条件**: 存在已配置的渠道
- **测试步骤**:
  1. 点击渠道的编辑按钮
  2. 修改渠道配置
  3. 保存配置
  4. 验证 API: `curl -X PATCH http://localhost:8000/api/channels/{type}` 返回结果
- **预期结果**: 渠道配置修改成功

### TC-043: 渠道测试连接
- **模块**: Channels
- **优先级**: P2
- **前置条件**: 存在已配置的渠道
- **测试步骤**:
  1. 点击渠道的"测试"按钮
  2. 验证 API: `curl -X POST http://localhost:8000/api/channels/{type}/test` 返回结果
  3. 检查测试结果提示（成功/失败）
- **预期结果**: 测试请求发送成功，返回连接测试结果

---

## 13. Skills (/skills)

### TC-044: 已安装 Skill 列表 (已验证 ✅)
- **模块**: Skills
- **优先级**: P0
- **前置条件**: 后端运行
- **测试步骤**:
  1. 访问 `/skills`
  2. 检查已安装 Skill 列表是否正常加载（页面不崩溃）
  3. 检查每个 Skill 是否显示名称、描述、版本等信息
  4. 验证 API: `curl http://localhost:8000/api/skills/installed` 返回 200
- **预期结果**: Skill 列表正确加载，页面不崩溃，Skill 信息完整

### TC-045: Skill 搜索 (已验证 ✅)
- **模块**: Skills
- **优先级**: P1
- **前置条件**: Skills 页面已加载
- **测试步骤**:
  1. 在搜索框输入关键词
  2. 检查搜索结果是否正确过滤
  3. 验证 API: `curl "http://localhost:8000/api/skills/search?q=test"` 返回 200
  4. 清空搜索，检查是否恢复全部列表
- **预期结果**: 搜索功能正常，结果正确，清空后恢复完整列表

### TC-046: Skill 安装/更新/卸载
- **模块**: Skills
- **优先级**: P1
- **前置条件**: Skills 页面已加载
- **测试步骤**:
  1. 搜索一个未安装的 Skill，点击安装
  2. 验证 API: `curl -X POST http://localhost:8000/api/skills/install` 
  3. 点击某个已安装 Skill 的"更新"按钮
  4. 验证 API: `curl -X POST http://localhost:8000/api/skills/update/{name}`
  5. 点击"卸载"按钮
  6. 验证 API: `curl -X POST http://localhost:8000/api/skills/uninstall/{name}`
- **预期结果**: 安装/更新/卸载操作正常，列表即时更新

---

## 14. Memory (/memory)

### TC-047: 记忆文件树展示 (已验证 ✅)
- **模块**: Memory
- **优先级**: P0
- **前置条件**: 后端运行
- **测试步骤**:
  1. 访问 `/memory`
  2. 检查 Agent 下拉选择器是否加载
  3. 选择一个 Agent 后检查文件树是否正确展示
  4. 验证 API: `curl "http://localhost:8000/api/memory/tree?agent=xxx"` 返回 200
- **预期结果**: 文件树正确展示，支持展开/折叠目录

### TC-048: 文件内容读取 (已验证 ✅)
- **模块**: Memory
- **优先级**: P0
- **前置条件**: 文件树已加载，存在至少1个文件
- **测试步骤**:
  1. 在文件树中点击一个文件
  2. 检查文件内容是否在右侧面板展示
  3. 验证 API: `curl "http://localhost:8000/api/memory/file?path=xxx"` 返回 200
- **预期结果**: 文件内容正确展示，格式保持（markdown 高亮等）

### TC-049: 文件删除
- **模块**: Memory
- **优先级**: P1
- **前置条件**: 文件树已加载，存在至少1个可删除文件
- **测试步骤**:
  1. 选中一个文件
  2. 点击删除按钮
  3. 确认删除
  4. 检查文件树是否更新
- **预期结果**: 文件删除成功，文件树即时更新

### TC-050: 记忆文件搜索
- **模块**: Memory
- **优先级**: P1
- **前置条件**: 存在记忆文件
- **测试步骤**:
  1. 在搜索框输入关键词
  2. 检查搜索结果是否正确匹配
  3. 点击搜索结果，检查是否跳转到对应文件
- **预期结果**: 搜索功能正常，结果准确

---

## 15. Usage (/usage)

### TC-051: 用量统计卡片展示 (已验证 ✅)
- **模块**: Usage
- **优先级**: P1
- **前置条件**: 后端运行
- **测试步骤**:
  1. 访问 `/usage`
  2. 检查统计卡片是否展示（总请求数、总 Token 等）
  3. 验证 API: `curl "http://localhost:8000/api/usage/summary?days=7"` 返回 200
- **预期结果**: 统计卡片正确展示关键用量指标

### TC-052: 时间范围切换 (已验证 ✅)
- **模块**: Usage
- **优先级**: P1
- **前置条件**: Usage 页面已加载
- **测试步骤**:
  1. 依次切换"今日"/"7天"/"30天"时间范围
  2. 每次切换后检查统计数据是否更新
  3. 验证 API: `curl "http://localhost:8000/api/usage/summary?days=1"` 返回 200
  4. 验证 API: `curl "http://localhost:8000/api/usage/sessions?days=7"` 返回 200
- **预期结果**: 不同时间范围的数据正确展示

### TC-053: Top Session 排行 (已验证 ✅)
- **模块**: Usage
- **优先级**: P1
- **前置条件**: 存在会话数据
- **测试步骤**:
  1. 在 Usage 页面查找 Top Session 排行榜
  2. 验证 API: `curl "http://localhost:8000/api/usage/sessions?days=7"` 返回 200
  3. 检查排行数据是否按用量排序
- **预期结果**: Top Session 列表正确按用量降序排列

### TC-054: 模型用量分布 (已验证 ✅)
- **模块**: Usage
- **优先级**: P1
- **前置条件**: 存在用量数据
- **测试步骤**:
  1. 在 Usage 页面查找模型用量图表
  2. 验证 API: `curl "http://localhost:8000/api/usage/by-model?days=7"` 返回 200
  3. 检查图表是否正确渲染不同模型的用量占比
- **预期结果**: 模型用量图表正确渲染，数据与 API 一致

---

## 16. Analytics/Cost (/analytics/cost)

### TC-055: 成本汇总展示 (已验证 ✅)
- **模块**: Analytics/Cost
- **优先级**: P1
- **前置条件**: 后端运行
- **测试步骤**:
  1. 访问 `/analytics/cost`
  2. 检查成本汇总卡片（总成本、本月成本等）
  3. 验证 API: `curl http://localhost:8000/api/analytics/cost/summary` 返回 200
- **预期结果**: 成本汇总数据正确展示

### TC-056: 成本趋势图表 (已验证 ✅)
- **模块**: Analytics/Cost
- **优先级**: P1
- **前置条件**: 存在成本数据
- **测试步骤**:
  1. 检查成本趋势图表是否渲染
  2. 验证 API: `curl "http://localhost:8000/api/analytics/cost/trend?period_days=30"` 返回 200
  3. 检查图表时间轴和数据点是否正确
- **预期结果**: 趋势图表正确渲染，数据点与 API 返回一致

### TC-057: Agent 成本分布 (已验证 ✅)
- **模块**: Analytics/Cost
- **优先级**: P1
- **前置条件**: 存在 Agent 成本数据
- **测试步骤**:
  1. 检查 Agent 成本排名表格
  2. 验证 API: `curl "http://localhost:8000/api/analytics/cost/by-agent?period_days=7"` 返回 200
- **预期结果**: Agent 成本排名正确展示

### TC-058: 预算管理
- **模块**: Analytics/Cost
- **优先级**: P2
- **前置条件**: Analytics 页面已加载
- **测试步骤**:
  1. 新建一个预算条目
  2. 切换预算启用/禁用状态
  3. 检查预算列表更新
- **预期结果**: 预算 CRUD 操作正常，状态切换即时生效

---

## 17. Logs (/logs)

### TC-059: 日志查看 (已验证 ✅)
- **模块**: Logs
- **优先级**: P1
- **前置条件**: 后端运行
- **测试步骤**:
  1. 访问 `/logs`
  2. 选择一个日志源
  3. 检查日志内容是否正确加载和展示
  4. 验证 API: `curl "http://localhost:8000/api/logs/tail?source=gateway&lines=200"` 返回 200
- **预期结果**: 日志按时间顺序正确展示，支持自动刷新

### TC-060: 日志源切换
- **模块**: Logs
- **优先级**: P1
- **前置条件**: Logs 页面已加载
- **测试步骤**:
  1. 查看可用的日志源列表
  2. 验证 API: `curl http://localhost:8000/api/logs/sources` 返回 200
  3. 切换不同日志源，检查日志内容是否更新
- **预期结果**: 日志源切换正常，不同源展示不同日志

### TC-061: 日志搜索与自动刷新
- **模块**: Logs
- **优先级**: P2
- **前置条件**: Logs 页面已加载，存在日志数据
- **测试步骤**:
  1. 在搜索框输入关键词
  2. 检查搜索结果是否正确过滤
  3. 开启自动刷新，检查日志是否每5秒更新
  4. 关闭自动刷新，确认停止更新
- **预期结果**: 搜索功能正常，自动刷新开关可用

---

## 18. Services (/services)

### TC-062: 服务状态展示 (已验证 ✅)
- **模块**: Services
- **优先级**: P1
- **前置条件**: 后端运行
- **测试步骤**:
  1. 访问 `/services`
  2. 检查服务状态卡片是否展示（Gateway、数据库等）
  3. 验证 API: `curl http://localhost:8000/api/services/status` 返回 200
- **预期结果**: 各服务状态正确展示（running/stopped），健康检查信息完整

### TC-063: 服务重启
- **模块**: Services
- **优先级**: P1
- **前置条件**: Services 页面已加载
- **测试步骤**:
  1. 点击重启按钮
  2. 确认重启操作
  3. 检查服务状态是否变化
  4. 等待重启完成后检查状态是否恢复
- **预期结果**: 重启操作执行成功，状态从 running → restarting → running

### TC-064: 服务配置查看
- **模块**: Services
- **优先级**: P2
- **前置条件**: Services 页面已加载
- **测试步骤**:
  1. 查看当前服务配置
  2. 检查配置历史记录
  3. 检查备份列表
- **预期结果**: 配置、历史、备份信息正确展示

---

## 19. Security (/security)

### TC-065: 安全概览 (已验证 ✅)
- **模块**: Security
- **优先级**: P1
- **前置条件**: 后端运行
- **测试步骤**:
  1. 访问 `/security`
  2. 检查安全概览信息（密码强度、IP 白名单状态等）
  3. 验证 API: `curl http://localhost:8000/api/security/status` 返回 200
  4. 验证 API: `curl http://localhost:8000/api/security/info` 返回 200
- **预期结果**: 安全概览信息完整展示

### TC-066: 密码修改
- **模块**: Security
- **优先级**: P1
- **前置条件**: 已知当前密码
- **测试步骤**:
  1. 输入当前密码和新密码
  2. 确认新密码
  3. 提交修改
  4. 验证 API: `curl -X POST http://localhost:8000/api/security/password`
- **预期结果**: 密码修改成功，错误密码应提示错误

### TC-067: IP 白名单管理
- **模块**: Security
- **优先级**: P2
- **前置条件**: Security 页面已加载
- **测试步骤**:
  1. 添加一个 IP 到白名单
  2. 检查白名单列表是否更新
  3. 删除该 IP
  4. 检查列表是否移除
- **预期结果**: 白名单增删正常，列表即时更新

---

## 20. Extensions (/extensions)

### TC-068: 扩展列表加载 (已验证 ✅)
- **模块**: Extensions
- **优先级**: P1
- **前置条件**: 后端运行
- **测试步骤**:
  1. 访问 `/extensions`
  2. 检查扩展列表是否正确加载
  3. 验证 API: `curl http://localhost:8000/api/extensions/list` 返回 200
- **预期结果**: 扩展列表正确展示，每个扩展显示名称、状态等

### TC-069: 扩展启用/禁用
- **模块**: Extensions
- **优先级**: P1
- **前置条件**: 存在至少1个扩展
- **测试步骤**:
  1. 点击扩展的启用/禁用切换
  2. 检查状态是否即时变化
  3. 刷新页面确认持久化
- **预期结果**: 扩展状态切换成功，持久化保存

### TC-070: Tunnel 状态
- **模块**: Extensions
- **优先级**: P2
- **前置条件**: Extensions 页面已加载
- **测试步骤**:
  1. 查看 Tunnel 状态面板
  2. 检查 Tunnel 连接信息是否展示
- **预期结果**: Tunnel 状态正确展示（连接/断开）

---

## 21. Communication (/communication)

### TC-071: 消息列表加载 (已验证 ✅)
- **模块**: Communication
- **优先级**: P1
- **前置条件**: 后端运行
- **测试步骤**:
  1. 访问 `/communication`
  2. 切换到"消息"Tab
  3. 检查消息列表是否加载
  4. 验证 API: `curl http://localhost:8000/api/communication/approvals` 返回 200
  5. 验证 API: `curl http://localhost:8000/api/communication/recent-messages` 返回 200
- **预期结果**: 消息列表正确加载，按时间排序

### TC-072: 广播消息
- **模块**: Communication
- **优先级**: P1
- **前置条件**: Communication 页面已加载
- **测试步骤**:
  1. 切换到"广播"功能
  2. 选择目标，输入消息内容
  3. 发送广播
- **预期结果**: 广播发送成功，目标收到消息

### TC-073: Webhook 管理 (已验证 ✅)
- **模块**: Communication
- **优先级**: P1
- **前置条件**: Communication 页面已加载
- **测试步骤**:
  1. 切换到 Webhook Tab
  2. 查看现有 Webhook 列表
  3. 验证 API: `curl http://localhost:8000/api/communication/hooks` 返回 200
  4. 添加新 Webhook
  5. 删除 Webhook
- **预期结果**: Webhook 增删正常，列表即时更新

### TC-074: 审批管理
- **模块**: Communication
- **优先级**: P2
- **前置条件**: 存在待审批项
- **测试步骤**:
  1. 查看待审批列表
  2. 执行审批操作（通过/拒绝）
  3. 检查列表更新
- **预期结果**: 审批操作正常，列表即时更新

---

## 22. 后端 API 健康检查

### TC-075: API 健康检查 (已验证 ✅)
- **模块**: 后端 API
- **优先级**: P0
- **前置条件**: 后端运行
- **测试步骤**:
  1. 验证 API: `curl http://localhost:8000/api/health` 返回 200
  2. 验证 API: `curl http://localhost:8000/api/ready` 返回 200
  3. 验证 API: `curl http://localhost:8000/` 返回 200
  4. 验证 API: `curl http://localhost:8000/docs` 返回 Swagger 文档页面
- **预期结果**: 所有健康检查端点正常返回

### TC-076: API CORS 配置
- **模块**: 后端 API
- **优先级**: P1
- **前置条件**: 后端运行，前端运行在不同端口
- **测试步骤**:
  1. 从前端域名发送跨域请求
  2. 检查响应头是否包含正确的 CORS 头
- **预期结果**: CORS 配置正确，前端可正常跨域访问后端 API

---

## 23. 数据一致性

### TC-077: Projects-Tasks 关联一致性
- **模块**: 数据一致性
- **优先级**: P0
- **前置条件**: 存在项目和任务数据
- **测试步骤**:
  1. 查询项目列表，记录每个项目的 taskCount
  2. 查询该项目下的任务数量
  3. 对比两者是否一致
  4. 删除项目时检查关联任务是否级联删除
- **预期结果**: 项目任务数统计准确，级联删除正常（P1-6 修复验证）

### TC-078: N+1 查询性能
- **模块**: 数据一致性
- **优先级**: P1
- **前置条件**: 存在大量项目和任务数据
- **测试步骤**:
  1. 调用 `GET /api/projects` 并检查响应时间
  2. 调用 `GET /api/tasks` 并检查响应时间
  3. 确认无 N+1 查询问题（P1-V2-1/P1-V2-2 修复验证）
- **预期结果**: 查询响应时间合理（<500ms），无 N+1 问题

---

## 24. 异常处理与边界情况

### TC-079: API 错误处理
- **模块**: 异常处理
- **优先级**: P1
- **前置条件**: 前端已启动
- **测试步骤**:
  1. 手动触发一个 404 错误（如访问不存在的任务 ID）
  2. 检查前端是否显示友好的错误提示
  3. 手动触发一个 422 错误（提交无效数据）
  4. 检查前端是否展示验证错误信息
- **预期结果**: API 错误被前端正确捕获，显示用户友好的错误提示

### TC-080: 空状态展示
- **模块**: 异常处理
- **优先级**: P1
- **前置条件**: 数据库清空或无数据
- **测试步骤**:
  1. 访问 Tasks 页面（无任务时）
  2. 访问 Projects 页面（无项目时）
  3. 访问 Skills 页面（无已安装 Skill 时）
  4. 检查每个页面是否有合理的空状态提示
- **预期结果**: 所有列表页面在无数据时展示空状态提示，无空白崩溃

### TC-081: 并发操作
- **模块**: 异常处理
- **优先级**: P2
- **前置条件**: 存在任务数据
- **测试步骤**:
  1. 同时打开两个浏览器 Tab
  2. 在 Tab A 编辑任务标题
  3. 在 Tab B 编辑同一任务的状态
  4. 保存后检查数据一致性
- **预期结果**: 后执行的更新覆盖先执行的，无数据损坏

### TC-082: 表单验证
- **模块**: 异常处理
- **优先级**: P1
- **前置条件**: 前端已启动
- **测试步骤**:
  1. 新建任务时不填写标题，提交表单
  2. 新建项目时输入非法 code（如含空格），提交表单
  3. 新建项目时输入重复 code，提交表单
  4. 检查各表单的验证提示
- **预期结果**: 表单验证正确拦截无效输入，显示明确的错误提示

---

## 25. WebSocket 代理

### TC-083: WebSocket 代理连接
- **模块**: WebSocket
- **优先级**: P0
- **前置条件**: 前端和后端均已运行
- **测试步骤**:
  1. 打开浏览器开发者工具 Network 面板
  2. 访问任意需要 Gateway 的页面（Dashboard/Chat/Sessions/Cron）
  3. 检查 WebSocket 连接是否建立到 `/ws/gateway`
  4. 检查连接状态是否为 `connected`
- **预期结果**: WebSocket 代理连接成功建立

### TC-084: WebSocket 重连机制
- **模块**: WebSocket
- **优先级**: P1
- **前置条件**: WebSocket 已连接
- **测试步骤**:
  1. 断开后端服务
  2. 观察前端重连行为（应自动重试，最多20次）
  3. 恢复后端服务
  4. 检查是否自动重连成功
- **预期结果**: 断连后自动重试，恢复后自动重连成功，重连延迟递增

---

## 统计

| 优先级 | 数量 |
|--------|------|
| P0 | 16 |
| P1 | 40 |
| P2 | 18 |
| **合计** | **84** |

> ✅ 已验证: 42 条用例的后端 API 已通过 curl 确认返回 200